# repos.toml -- Repository configuration for the Thrum engine.
#
# Thrum manages itself. Bootstrap configuration.

[[repo]]
name = "thrum"
path = "/Users/r/git/automator"
build_cmd = "cargo build"
test_cmd = "cargo test"
lint_cmd = "cargo clippy --workspace --tests -- -D warnings"
fmt_cmd = "cargo fmt -- --check"
claude_md = "/Users/r/git/automator/CLAUDE.md"
